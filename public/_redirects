# Cloudflare Pages / Netlify redirect: bare root → default locale (RU).
/  /ru/  302

# Clean public entry for the OAuth MCP connector → the Cloudflare Worker that hosts it.
# 307 preserves the method + body (the MCP endpoint is POST). The OAuth flow itself still
# runs on the worker's own domain (it bakes its URLs into the metadata); this only makes
# the address users add to Claude live on the project domain.
/mcp  https://macc-mcp.nposypanko.workers.dev/mcp  307
