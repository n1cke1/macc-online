# Cloudflare Pages / Netlify redirect: bare root → default locale (RU).
/  /ru/  302

# NOTE: the MCP connector URL is the Worker directly (macc-mcp.…workers.dev/mcp), NOT a
# /mcp redirect here — an OAuth client discovers the auth-server metadata at the URL's own
# origin, and a static domain can't serve it. A clean URL needs a custom domain on the
# Worker, not a redirect.
